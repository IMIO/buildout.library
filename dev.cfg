[buildout]
extends =
    base.cfg

extensions =
    mr.developer

auto-checkout =
    library.policy
    library.theme
    collective.behavior.banner

[instance]
debug-mode = on
verbose-security = on
user = admin:admin
eggs +=
    ${debug-products:eggs}
